meta {
  name: List Untracked Recordings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/recordings/untracked
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("response is array", function() {
    expect(res.body).to.be.an('array');
  });
}
