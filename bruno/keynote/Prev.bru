meta {
  name: Keynote Previous Slide
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/keynote/prev
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
