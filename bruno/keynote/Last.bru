meta {
  name: Keynote Last Slide
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/keynote/last
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
