meta {
  name: Keynote Next Slide
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/keynote/next
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
