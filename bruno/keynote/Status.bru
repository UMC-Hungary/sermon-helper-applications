meta {
  name: Keynote Status
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/keynote/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
