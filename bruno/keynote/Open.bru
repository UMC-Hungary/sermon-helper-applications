meta {
  name: Keynote Open
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/keynote/open
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "file_path": "/Users/admin/Presentations/Sermon.key"
  }
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
