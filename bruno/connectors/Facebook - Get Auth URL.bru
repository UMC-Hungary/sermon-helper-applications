meta {
  name: Facebook - Get Auth URL
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/auth/facebook/url
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("response has url", function() {
    expect(res.body).to.have.property('url');
  });
}
