meta {
  name: YouTube - Schedule Event
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/connectors/youtube/schedule/{{eventId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
