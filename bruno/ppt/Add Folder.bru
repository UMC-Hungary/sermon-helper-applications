meta {
  name: Add PPT Folder
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/ppt/folders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "path": "/Users/admin/Presentations",
    "name": "Sunday Services"
  }
}

tests {
  test("status is 201", function() {
    expect(res.status).to.equal(201);
  });
}
