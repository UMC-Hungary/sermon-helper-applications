meta {
  name: List Broadlink Devices
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/connectors/broadlink/devices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("response is array", function() {
    expect(res.body).to.be.an('array');
  });
}
