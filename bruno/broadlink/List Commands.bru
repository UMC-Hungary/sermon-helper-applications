meta {
  name: List Broadlink Commands
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/connectors/broadlink/commands
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("response is array", function() {
    expect(res.body).to.be.an('array');
  });
}
