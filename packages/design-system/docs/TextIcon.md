<!-- GENERATED by scripts/build-docs.mjs from packages/design-system/src/primitives/TextIcon.svelte, docs/specs.json and tokens/fidelity.json. Edit those, not this. -->

# TextIcon

A single typographic character standing in for an icon, in the display face, sized to match `Icon`.

## Anatomy

One character centred in a square.

## Props

| Prop    | Type     | Default      | Meaning                                                                          |
| ------- | -------- | ------------ | -------------------------------------------------------------------------------- |
| `char`  | `string` | `'·', label` | A single typographic character standing in for an icon, set in the display face. |
| `label` | `string` | —            | Give a name only when the character is the sole carrier of meaning.              |

## Variants

One per character.

## States

Static.

## Tokens consumed

| Token                     | Resolves to                            |
| ------------------------- | -------------------------------------- |
| `--c-text-icon-font-size` | `18px`                                 |
| `--c-text-icon-size`      | `28px`                                 |
| `--text-primary`          | `#1c1a16 / #ede6d6`                    |
| `--type-heading-family`   | `"Cormorant Garamond", Georgia, serif` |

Every value resolves through a semantic or component token; `scripts/check-fidelity.mjs` fails on a literal.

## Keyboard

Not interactive.

## ARIA

Decorative unless given a `label`.

## Accessibility acceptance criteria

- Reachable, operable and leavable by keyboard alone, with a focus indicator meeting 3:1 in both schemes.
- An accessible name for every control, and current state exposed programmatically.
- No meaning carried by colour alone.
- Legible and operable at a 360px viewport and at desktop width, and at 200% zoom.
- Interactive targets at least 44 × 44px on touch-capable viewports.

## Reference correspondence

Source: `src/components/primitives/TextIcon.svelte`.

| Selector | Property    | Measured | Implemented | Token                     |
| -------- | ----------- | -------- | ----------- | ------------------------- |
| `span`   | `width`     | `28px`   | `28px`      | `--c-text-icon-size`      |
| `span`   | `font-size` | `18px`   | `18px`      | `--c-text-icon-font-size` |

Re-checked mechanically by `scripts/check-fidelity.mjs`.

## Recorded deviations

None.
