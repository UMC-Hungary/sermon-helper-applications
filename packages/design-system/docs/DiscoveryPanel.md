<!-- GENERATED by scripts/build-docs.mjs from packages/design-system/src/patterns/DiscoveryPanel.svelte, docs/specs.json and tokens/fidelity.json. Edit those, not this. -->

# DiscoveryPanel

A bordered panel that scans for something on the network and shows what it finds.

## Anatomy

A header — mono title, prose description, and the scan action — over a hairline rule, then the body, which shows a `Spinner` while scanning and its children otherwise.

## Props

| Prop                     | Type         | Default      | Meaning                                                       |
| ------------------------ | ------------ | ------------ | ------------------------------------------------------------- |
| `title` _(required)_     | `string`     | —            | The mono heading — "Broadlink devices".                       |
| `description`            | `string`     | `''`         | The prose line under it, set in the reference's serif italic. |
| `scanLabel` _(required)_ | `string`     | —            | —                                                             |
| `scanning`               | `boolean`    | `false`      | —                                                             |
| `scanningLabel`          | `string`     | `'Scanning'` | Announced while a scan runs, and shown beside the spinner.    |
| `onscan`                 | `() => void` | —            | —                                                             |
| `children`               | `Snippet`    | —            | —                                                             |

## Variants

None.

## States

Idle, scanning, populated, empty.

## Tokens consumed

| Token                          | Resolves to                                                    |
| ------------------------------ | -------------------------------------------------------------- |
| `--accent`                     | `#5c5c84 / #a9a6d6`                                            |
| `--border-hairline`            | `rgba(28, 26, 22, 0.1) / rgba(237, 230, 214, 0.09)`            |
| `--c-discovery-body-padding`   | `2px`                                                          |
| `--c-discovery-button-padding` | `10px`                                                         |
| `--c-discovery-button-track`   | `1.2px`                                                        |
| `--c-discovery-header-gap`     | `10px`                                                         |
| `--c-discovery-status-padding` | `12px`                                                         |
| `--c-discovery-title-size`     | `9.5px`                                                        |
| `--c-discovery-title-track`    | `1.4px`                                                        |
| `--c-field-hint-track`         | `1.2px`                                                        |
| `--c-row-meta-gap`             | `4px`                                                          |
| `--surface-inverse`            | `#1c1a16 / #ede6d6`                                            |
| `--surface-raised`             | `#f6f2e8 / #1e1b16`                                            |
| `--text-inverse`               | `#efe9dd / #14120f`                                            |
| `--text-muted`                 | `#615e56 / #918b7e`                                            |
| `--text-secondary`             | `#5c574e / #a8a294`                                            |
| `--type-body-sm-leading`       | `1.4`                                                          |
| `--type-label-family`          | `"Geist Mono", ui-monospace, SFMono-Regular, Menlo, monospace` |
| `--type-label-size`            | `10px`                                                         |
| `--type-label-sm-family`       | `"Geist Mono", ui-monospace, SFMono-Regular, Menlo, monospace` |
| `--type-label-sm-transform`    | `uppercase`                                                    |
| `--type-label-transform`       | `uppercase`                                                    |
| `--type-quote-family`          | `"Cormorant Garamond", Georgia, serif`                         |
| `--type-quote-size`            | `13px`                                                         |
| `--ui-border-hairline`         | `1px`                                                          |
| `--ui-focus-offset`            | `2px`                                                          |
| `--ui-focus-width`             | `2px`                                                          |
| `--ui-gutter-inset`            | `14px`                                                         |
| `--ui-gutter-tight`            | `12px`                                                         |
| `--ui-stack`                   | `8px`                                                          |
| `--ui-target-min`              | `44px`                                                         |

Every value resolves through a semantic or component token; `scripts/check-fidelity.mjs` fails on a literal.

## Keyboard

| Key              | Behaviour                               |
| ---------------- | --------------------------------------- |
| `Tab`            | Reaches the scan action, then the body. |
| `Enter`, `Space` | Starts a scan.                          |

## ARIA

The scan action is disabled while a scan runs; the `Spinner`'s status region announces that it is running, so the wait is never silent.

## Accessibility acceptance criteria

- Reachable, operable and leavable by keyboard alone, with a focus indicator meeting 3:1 in both schemes.
- An accessible name for every control, and current state exposed programmatically.
- No meaning carried by colour alone.
- Legible and operable at a 360px viewport and at desktop width, and at 200% zoom.
- Interactive targets at least 44 × 44px on touch-capable viewports.

## Reference correspondence

Source: `src/components/connectors/BroadlinkDiscovery.svelte`.

| Selector                        | Property         | Measured | Implemented | Token                          |
| ------------------------------- | ---------------- | -------- | ----------- | ------------------------------ |
| `.panel`                        | `padding`        | `14px`   | `14px`      | `--ui-gutter-inset`            |
| `.panel`                        | `border`         | `1px`    | `1px`       | `--ui-border-hairline`         |
| `header`                        | `gap`            | `12px`   | `12px`      | `--ui-gutter-tight`            |
| `header`                        | `margin-bottom`  | `10px`   | `10px`      | `--c-discovery-header-gap`     |
| `strong`                        | `font-size`      | `9.5px`  | `9.5px`     | `--c-discovery-title-size`     |
| `strong`                        | `letter-spacing` | `1.4px`  | `1.4px`     | `--c-discovery-title-track`    |
| `header span, .empty`           | `margin-top`     | `4px`    | `4px`       | `--c-row-meta-gap`             |
| `header span, .empty`           | `font-size`      | `13px`   | `13px`      | `--type-quote-size`            |
| `header span, .empty`           | `line-height`    | `1.4`    | `1.4`       | `--type-body-sm-leading`       |
| `header button, article button` | `padding`        | `10px`   | `10px`      | `--c-discovery-button-padding` |
| `header button, article button` | `letter-spacing` | `1.2px`  | `1.2px`     | `--c-discovery-button-track`   |
| `.body`                         | `padding-top`    | `2px`    | `2px`       | `--c-discovery-body-padding`   |
| `.status`                       | `gap`            | `8px`    | `8px`       | `--ui-stack`                   |
| `.status`                       | `padding`        | `12px`   | `12px`      | `--c-discovery-status-padding` |
| `.status`                       | `font-size`      | `10px`   | `10px`      | `--type-label-size`            |
| `.status`                       | `letter-spacing` | `1.2px`  | `1.2px`     | `--c-field-hint-track`         |

Re-checked mechanically by `scripts/check-fidelity.mjs`.

## Recorded deviations

None.
