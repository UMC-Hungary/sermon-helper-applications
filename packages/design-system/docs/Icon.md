<!-- GENERATED by scripts/build-docs.mjs from packages/design-system/src/primitives/Icon.svelte, docs/specs.json and tokens/fidelity.json. Edit those, not this. -->

# Icon

The line-drawn marks the interface uses. Twenty-four in a single 24×24 stroked style, so a size change never changes the drawing's weight.

## Anatomy

One `<path>` in a 24-unit viewBox, stroked in `currentColor`.

## Props

| Prop                | Type       | Default | Meaning                                                                                |
| ------------------- | ---------- | ------- | -------------------------------------------------------------------------------------- |
| `name` _(required)_ | `IconName` | —       | —                                                                                      |
| `size`              | `number`   | —       | Matches the reference's per-call sizing; the drawing scales, the stroke does not.      |
| `stroke`            | `number`   | —       | —                                                                                      |
| `label`             | `string`   | —       | Decorative by default. Give a label only when the icon is the sole carrier of meaning. |

## Variants

One per name; see the `icons` export for the set.

## States

Static.

## Tokens consumed

None — the component carries no styles of its own.

## Keyboard

Not interactive.

## ARIA

Decorative by default — `aria-hidden` with `role="presentation"`. Given a `label` it becomes an `img` with a title. An icon inside a labelled control must stay decorative, or the control is announced twice.

## Accessibility acceptance criteria

- Reachable, operable and leavable by keyboard alone, with a focus indicator meeting 3:1 in both schemes.
- An accessible name for every control, and current state exposed programmatically.
- No meaning carried by colour alone.
- Legible and operable at a 360px viewport and at desktop width, and at 200% zoom.
- Interactive targets at least 44 × 44px on touch-capable viewports.

## Reference correspondence

Carried over from `src/components/primitives/Icon.svelte`. Its sixteen marks are unchanged; eight more — `down`, `up`, `close`, `check`, `minus`, `alert`, `info`, `retry` — are drawn in the same style for the components the reference never had. Four of the carried-over marks were redrawn as single paths so the set has one shape per name.

## Recorded deviations

| What                          | Why                                                                                                                             |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| The set is one path per icon. | The reference draws some marks as a `rect` plus a `path`, which makes a stroke-width change land differently on the two halves. |
