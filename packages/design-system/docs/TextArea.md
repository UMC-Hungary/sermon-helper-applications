<!-- GENERATED by scripts/build-docs.mjs from packages/design-system/src/primitives/TextArea.svelte, docs/specs.json and tokens/fidelity.json. Edit those, not this. -->

# TextArea

Multi-line text. Used with a `FormField`, which owns the label and the notes.

## Anatomy

A bordered field on the sunken surface, resizable vertically.

## Props

| Prop | Type | Default | Meaning |
| --- | --- | --- | --- |
| `value` | `string` | `$bindable('')` | — |
| `placeholder` | `string` | `''` | — |
| `rows` | `number` | `4` | — |
| `disabled` | `boolean` | `false` | — |
| `readonly` | `boolean` | `false` | — |
| `required` | `boolean` | `false` | — |
| `invalid` | `boolean` | `false` | — |
| `id` | `string` | — | — |
| `describedby` | `string` | — | — |
| `label` | `string` | — | — |

## Variants

None.

## States

Default, focus-visible, readonly, disabled, invalid.

## Tokens consumed

| Token | Resolves to |
| --- | --- |
| `--accent` | `#5c5c84 / #a9a6d6` |
| `--border-control` | `#857c68 / #716b5c` |
| `--c-text-field-padding-block` | `9px` |
| `--status-error` | `#b5321c / #e66a4f` |
| `--surface-sunken` | `#e4ddcd / #23201a` |
| `--text-faint` | `#676051 / #8f8879` |
| `--text-muted` | `#615e56 / #918b7e` |
| `--text-primary` | `#1c1a16 / #ede6d6` |
| `--type-body-family` | `"Inter Tight", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` |
| `--type-body-size` | `15px` |
| `--type-body-track` | `-0.1px` |
| `--type-caption-leading` | `1.45` |
| `--ui-border-hairline` | `1px` |
| `--ui-focus-offset` | `2px` |
| `--ui-focus-width` | `2px` |
| `--ui-gutter-inset` | `14px` |
| `--ui-radius-square` | `0` |

Every value resolves through a semantic or component token; `scripts/check-fidelity.mjs` fails on a literal.

## Keyboard

| Key | Behaviour |
| --- | --- |
| `Tab` | Moves to and from the field. |

## ARIA

A native `<textarea>`. It applies whatever `id`, `describedby` and `invalid` its `FormField` gives it; standing alone it takes an `aria-label`.

## Accessibility acceptance criteria

- Reachable, operable and leavable by keyboard alone, with a focus indicator meeting 3:1 in both schemes.
- An accessible name for every control, and current state exposed programmatically.
- No meaning carried by colour alone.
- Legible and operable at a 360px viewport and at desktop width, and at 200% zoom.
- Interactive targets at least 44 × 44px on touch-capable viewports.

## Reference correspondence

None. The reference has no multi-line input; the treatment is `LabelledInput`'s, at body leading.

## Recorded deviations

None.

