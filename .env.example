# Bible API Configuration
# These settings are only used in browser mode (pnpm dev)
# When running as Tauri app (pnpm tauri dev), requests go through Rust and bypass CORS

# V2 API URL (nyiregyhazimetodista.hu) - supports UF, RUF translations
VITE_BIBLE_V2_API_URL=https://api.nyiregyhazimetodista.hu

# Legacy API URL (szentiras.eu) - supports RUF, KG, KNB, SZIT, BD, STL translations
VITE_BIBLE_LEGACY_API_URL=https://szentiras.eu

# CORS Proxy Configuration (browser mode only)
# Set to 'false' to disable CORS proxy (will fail if szentiras.eu blocks requests)
VITE_USE_CORS_PROXY=true

# CORS Proxy URL - used to bypass CORS restrictions in browser mode
# Available options:
#   - https://corsproxy.io/?              (default, free, reliable)
#   - https://api.allorigins.win/raw?url= (alternative)
#   - Your own proxy server URL
VITE_CORS_PROXY_URL=https://corsproxy.io/?
